apiVersion: v1
kind: Service
metadata:
  name: question-service
  labels:
    app: question
    component: api
    tier: backend
    environment: development
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
  selector:
    app: question
    component: api
    tier: backend
    environment: development
