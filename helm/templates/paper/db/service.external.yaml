apiVersion: v1
kind: Service
metadata:
  name: external-paper-db-service
  labels:
    app: paper
    component: api
    tier: database
    environment: development
spec:
  type: ExternalName
  externalName: host.docker.internal
