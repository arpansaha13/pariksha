apiVersion: v1
kind: Service
metadata:
  name: exam-queue-service
  labels:
    app: exam-queue
    component: broker
    tier: backend
    environment: development
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: exam-queue
    component: broker
    tier: backend
    environment: development
