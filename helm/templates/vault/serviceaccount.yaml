{{- if .Values.vault.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault
  namespace: {{ .Release.Namespace }}
  labels:
    app: vault
    component: storage
    tier: backend
    environment: {{ .Values.global.environment }}
{{- end }}
