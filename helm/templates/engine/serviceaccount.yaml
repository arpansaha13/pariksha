{{- if .Values.engine.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: engine-serviceaccount
  labels:
    app: engine
    component: api
    tier: backend
    environment: {{ .Values.global.environment }}
{{- end }}
